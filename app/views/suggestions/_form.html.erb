<% @suggestion.errors.full_messages.each do |msg| %>
  <%= msg %>
<% end %>

<%= simple_form_for :suggestion, url: api_v1_suggestions_path do |f| %>
  <%= f.input :description, as: :text %>

  <button class='waves-effect waves-light btn-large'>Submit</button>

<% end %>
